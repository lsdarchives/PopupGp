# PopupGp

1. Setting up a GitHub repo
1.2 Clone the GitHub repo on Cursor
   
2. Setting up mobile-app(react native-expo) 
  * npx create-expo-app mobile-app
    
3. npx expo start -c

4. firebase auth + login screen
   * Login screen UI
   * Firebase authentication connection
   * Basic user session flow
   * Foundation for user/host/admin roles later
  
******************************************************
due to sdk mismatch with expo go app, i swtched to EAS Dev Build which uses  ________.
1. npx expo install expo-dev-client
   
2. consfigure EAS
   * npx eas build:configure
   * i configured the project as a android build and eas.json was generated
   * simillar to expo, using eas also requires downloading an apk which will
     be provided duirng configuration. le
     
